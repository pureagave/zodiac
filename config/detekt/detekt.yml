style:
  MagicNumber:
    active: false
  MaxLineLength:
    active: false

complexity:
  LongMethod:
    active: false
  TooManyFunctions:
    # Default 11 was tight for CockpitViewModel which exposes one method per
    # discrete cockpit operation. Bumped each time a discrete cockpit action
    # is added (concept cycling was the latest); kept just above the actual
    # count so adding a 14th would still require a deliberate decision.
    thresholdInClasses: 13

naming:
  FunctionNaming:
    functionPattern: '[a-zA-Z][a-zA-Z0-9]*'
