package ai.openclaw.zodiaccontrol.core.model

/**
 * How the BRC map is presented in the cockpit's center viewport.
 * - [TOP] orthographic top-down (default).
 * - [TILT] a ~40° pitched 3D view with a retro perspective grid backdrop.
 */
enum class MapMode { TOP, TILT }
